## CONTEXTE

Générer des tests fait gagner du temps sur les cas évidents, mais l'IA rate les cas limites propres à ton domaine (projet sans `github`, projet avec `demo` mais sans dépôt public).

## APPLICATION

- Fais générer une suite de tests pour `searchProjects`.
- Ajoute toi-même trois cas limites que l'IA a manqués, tirés de tes vraies données.
- Vérifie qu'aucun test généré ne teste l'implémentation plutôt que le comportement.

## Vérification

Quel cas limite propre à ton catalogue l'IA ne pouvait pas deviner ?

## 🎬 Ta suite de tests est complétée là où ça compte

Vitesse de l'IA plus ta connaissance du domaine : c'est la bonne combinaison. Commit.
