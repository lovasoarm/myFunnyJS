# Vérification : 13_design_patterns

3 drills à sortie déterministe. Écris ton `solution.js`, puis :

```bash
bash verification_pack/13_design_patterns/verify.sh path/to/solution.js
```

Chaque drill lit `inputs/drill_N.txt` sur stdin et doit produire EXACTEMENT `expected/drill_N.txt` sur stdout.

Les contrats précis (format d'entrée, format de sortie) sont à définir par le mainteneur du module. Consulte le `README.md` du module pour la spec exacte.
